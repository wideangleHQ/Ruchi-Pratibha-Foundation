export interface StatItem {
  value: string;
  numericValue: number;
  suffix: string;
  label: string;
  sublabel?: string;
}

export interface CredentialItem {
  title: string;
  description: string;
}
